# pr-assistant-standard

نسخهٔ پایه از PR Assistant — توضیحات و نحوهٔ راه‌اندازی.

## Quick start (local)

```bash
python -m venv venv
source venv/bin/activate    # linux / mac
venv\Scripts\activate       # windows
pip install -r requirements.txt
cp .env.example .env
# ویرایش .env با مقادیر واقعی
